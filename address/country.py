# Import the Enum class from the enum module
from enum import Enum

# Define the Country class as an enumeration
class Country(Enum):
    CANADA = "CA"  # Canada is represented by the string "CA"
    USA  = "US"  # The United States is represented by the string "US"
    INDIA = "IN"  # India is represented by the string "IN"